// Spending pattern helpers — placeholder for future work (no Phase 3 task ID
// yet). Kept as a stub so the module surface is stable.

export function notImplemented() {
  throw new Error('analytics/patterns: not implemented');
}
